### Переключиться на другую схему
SET search_path=operations;

### Пример переименования колонки
ALTER TABLE queue.qr_notice_reject_operation RENAME COLUMN try_time TO trx_time; 

### Пример альтера колонки
ALTER TABLE reports.transactions ALTER COLUMN terminal_id SET DATA TYPE varchar(15);
ALTER TABLE reports.transactions ALTER COLUMN terminal_id TYPE varchar(15);

### Пример дроп констрейнта
ALTER TABLE qr_transfer.transactions ALTER COLUMN request_id DROP NOT NULL
ALTER TABLE qr_transfer.transactions ALTER COLUMN request_id SET NOT NULL

### Пример альтера с изменением типа столбца
ALTER TABLE qrhub_sbp.entity ALTER COLUMN active TYPE boolean USING active::boolean;

### Пример альтера с изменением владельца таблицы
ALTER TABLE queue_notice OWNER TO ph_user;э

### Создание базового индекса
CREATE INDEX collector_external_unique_key ON operations.collector_external USING btree (id_point, operation_number);