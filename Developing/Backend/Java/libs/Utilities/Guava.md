# 📚 Guava

## 📝 Описание
Guava — это мощная библиотека от Google, расширяющая стандартную библиотеку Java. Она содержит коллекции, кэширование, поддержку функционального программирования, работу со строками, обработку ошибок, и многое другое.

## 🚀 Основные классы / интерфейсы
- `ImmutableList` — неизменяемый список, повышающий безопасность и производительность
- `Optional` — безопасная обёртка для работы с возможным `null`
- `Preconditions` — удобные проверки аргументов и состояний
- `Multimap` — расширенная коллекция Map, поддерживающая несколько значений на один ключ
- `ListenableFuture` — расширение `Future`, добавляющее возможность добавлять колбэки

## 🔑 Ключевые методы
- `Preconditions.checkNotNull(obj)` — выбрасывает исключение, если объект `null`
- `Optional.of(value).get()` — извлекает значение из Optional, если оно есть
- `ImmutableList.of("a", "b", "c")` — создаёт неизменяемый список
- `Strings.isNullOrEmpty(str)` — проверяет строку на null или пустоту
- `CacheBuilder.newBuilder().maximumSize(1000).build()` — создаёт настраиваемый кэш

## 💡 Примеры использования
```java
import com.google.common.base.Optional;
import com.google.common.collect.ImmutableList;
import com.google.common.base.Preconditions;
import com.google.common.collect.Multimap;
import com.google.common.collect.ArrayListMultimap;

public class GuavaDemo {
    public static void main(String[] args) {
        // Optional
        Optional<String> name = Optional.of("Guava");
        System.out.println(name.get());

        // Preconditions
        String input = "Hello";
        Preconditions.checkNotNull(input, "Input cannot be null");

        // ImmutableList
        ImmutableList<String> list = ImmutableList.of("A", "B", "C");
        System.out.println(list);

        // Multimap
        Multimap<String, String> multimap = ArrayListMultimap.create();
        multimap.put("fruit", "apple");
        multimap.put("fruit", "banana");
        System.out.println(multimap.get("fruit"));
    }
}
```

## ⚙️ Настройки / расширения
- Guava не требует конфигурации, но можно подключать только нужные модули (например, `guava-collections`, `guava-cache`)
- Кэш `CacheBuilder` поддерживает гибкие политики: `expireAfterWrite`, `refreshAfterWrite`, `maximumSize`, и т.д.

## ❗ Подводные камни / особенности
- Некоторые части библиотеки помечены как `@Beta` — могут изменяться без предупреждения
- Неизменяемые коллекции Guava **не поддерживают null**
- Использование `Optional` из Guava нежелательно в новых проектах — предпочтительнее `java.util.Optional` (Java 8+)

## 🔗 Ссылки
- [Документация](https://github.com/google/guava/wiki?utm_source=chatgpt.com)
- [Пример на GitHub](https://github.com/google/guava?utm_source=chatgpt.com)

## 👨‍🦽‍➡️Своими словами
- Сильно упрощает код, избавляя от рутинных проверок и шаблонов, добавляет неизменяемый список и мультимапу, а также кэш.