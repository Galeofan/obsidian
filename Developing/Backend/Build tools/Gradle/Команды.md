### Запуск таска gretty c выводом логов
./gradlew appRun --info 
#### Проверка кто притянул http-клиент
./gradlew dependencyInsight \
  --dependency httpclient \
  --configuration testRuntimeClasspath

### Проверка дерева зависимостей
./gradlew dependencies

### Пример запуска тестов вручную через CLI
```bash
export JAVA_HOME="/c/java/jdk-11.0.24"
export PATH="$JAVA_HOME/bin:$PATH"
cd /c/git/ph/softlogic/gates/providers/multitransfer-transfer-out
/c/java/gradle-5.4.1/bin/gradle test --tests --stacktrace "ru.mtsbank.processing.gates.multitransfer.transfer.out.util.JsonSchemaFlattenerTest"
```