# 📚 Apache Commons Lang

## 📝 Описание
Apache Commons Lang — это библиотека, расширяющая стандартный функционал Java. Она содержит утилиты для работы со строками, объектами, числами, датами, перечислениями, рефлексией и многим другим. Библиотека значительно упрощает повседневные задачи и повышает читаемость кода.

## 🚀 Основные классы / интерфейсы
- `StringUtils` — предоставляет более 100 статических методов для работы со строками (null-safe, trimming, padding, splitting и т.д.)
- `ObjectUtils` — утилиты для безопасной работы с объектами, включая null-check'и и сравнение
- `ArrayUtils` — удобная работа с массивами (объединение, копирование, проверка наличия элемента и пр.)
- `NumberUtils` — парсинг чисел, сравнение, генерация случайных чисел
- `ReflectionToStringBuilder` — генерация `toString()` с использованием рефлексии

## 🔑 Ключевые методы
- `StringUtils.isBlank(String)` — проверяет, пуста ли строка (включая null и пробелы)
- `StringUtils.join(List, String)` — объединяет список строк в одну строку через разделитель
- `ObjectUtils.defaultIfNull(obj, default)` — возвращает объект или значение по умолчанию, если он null
- `ArrayUtils.contains(array, element)` — проверяет, содержится ли элемент в массиве
- `NumberUtils.toInt(String, int defaultValue)` — безопасно преобразует строку в число

## 💡 Примеры использования
```java
import org.apache.commons.lang3.StringUtils;
import org.apache.commons.lang3.ObjectUtils;
import org.apache.commons.lang3.ArrayUtils;
import org.apache.commons.lang3.math.NumberUtils;

public class CommonsLangExample {
    public static void main(String[] args) {
        // StringUtils
        System.out.println(StringUtils.isBlank("   ")); // true
        System.out.println(StringUtils.join(new String[]{"A", "B", "C"}, ", ")); // "A, B, C"

        // ObjectUtils
        String value = null;
        String safe = ObjectUtils.defaultIfNull(value, "default");
        System.out.println(safe); // "default"

        // ArrayUtils
        int[] numbers = {1, 2, 3};
        System.out.println(ArrayUtils.contains(numbers, 2)); // true

        // NumberUtils
        int result = NumberUtils.toInt("not_a_number", 42);
        System.out.println(result); // 42
    }
}
```

## ⚙️ Настройки / расширения
```
<dependency>
    <groupId>org.apache.commons</groupId>
    <artifactId>commons-lang3</artifactId>
    <version>3.13.0</version>
</dependency>

implementation 'org.apache.commons:commons-lang3:3.13.0'
```
## ❗ Подводные камни / особенности
- Некоторые методы могут быть избыточны, если используется Java 11+ (например, `String::isBlank` уже есть в стандартной библиотеке)
- Методы работают с null, но не всегда одинаково — важно читать документацию
- Не стоит злоупотреблять `ReflectionToStringBuilder` в продакшене из-за потенциальной утечки чувствительных данных

## 🔗 Ссылки
- [Документация](https://commons.apache.org/proper/commons-lang/?utm_source=chatgpt.com)
- [Пример на GitHub](https://github.com/apache/commons-lang?utm_source=chatgpt.com)

## 👨‍🦽‍➡️Своими словами
- Библиотека реализует шаблонные методы для работы со строками, числами, объектами, массивами, Enum и т.д.

## Ещё полезные методы
### 📌 StringUtils — работа со строками

Полностью null-safe ✅, экономит кучу кода.

|Метод|Назначение|Пример|
|---|---|---|
|`isBlank(str)`|`null`, пустая строка или только пробелы?|`true` ← `StringUtils.isBlank(" ")`|
|`isNotBlank(str)`|Обратное `isBlank()`|`true` ← `StringUtils.isNotBlank("hi")`|
|`join(array, sep)`|Склеить массив строк с разделителем|`"a, b"` ← `join(["a","b"], ", ")`|
|`split(str, sep)`|Разделить строку по символу|`["a", "b"]` ← `split("a:b", ":")`|
|`capitalize(str)`|Сделать первую букву заглавной|`"Hello"` ← `capitalize("hello")`|
|`uncapitalize(str)`|Первая буква — строчной|`"java"` ← `uncapitalize("Java")`|
|`reverse(str)`|Перевернуть строку|`"cba"` ← `reverse("abc")`|
|`abbreviate(str, len)`|Обрезать и добавить `...`|`"Hello..."` ← `abbreviate("Hello World", 8)`|

---

### 📌 ObjectUtils — работа с объектами

Минимизирует `NullPointerException` 🤕

|Метод|Назначение|Пример|
|---|---|---|
|`defaultIfNull(obj, def)`|Вернуть `obj` или `def`, если `obj == null`|`"fallback"` ← `defaultIfNull(null, "fallback")`|
|`firstNonNull(a, b, c...)`|Первый != null из аргументов|`"x"` ← `firstNonNull(null, "x", "y")`|
|`allNotNull(objs...)`|true, если все НЕ null|`true` ← `allNotNull("a", 1, new Object())`|
|`anyNotNull(objs...)`|true, если ХОТЯ БЫ один не null|`true` ← `anyNotNull(null, "value")`|

---

### 📌 ArrayUtils — работа с массивами

Полезен при работе с `int[]`, `Object[]`, и т.д.

|Метод|Назначение|Пример|
|---|---|---|
|`contains(arr, el)`|Проверка на наличие|`true` ← `contains([1,2,3], 2)`|
|`add(arr, el)`|Добавить элемент|`[1,2,3]` ← `add([1,2], 3)`|
|`removeElement(arr, el)`|Удалить элемент|`[1,3]` ← `removeElement([1,2,3], 2)`|
|`isEmpty(arr)`|Проверка на null/пустоту|`true` ← `isEmpty(null)`|

---

### 📌 NumberUtils — числа и безопасный парсинг

Избавляет от `NumberFormatException` 💥

|Метод|Назначение|Пример|
|---|---|---|
|`toInt(str, def)`|Преобразовать строку в `int` или вернуть `def`|`10` ← `toInt("10", 0)`|
|`toDouble(str, def)`|Аналогично для `double`|`1.5` ← `toDouble("1.5", 0)`|
|`isDigits(str)`|Только цифры?|`true` ← `isDigits("123")`|
|`isCreatable(str)`|Можно ли сконвертировать в число?|`true` ← `isCreatable("-0xFF")`|

---

### 📌 RandomStringUtils — генерация случайных строк
Нужна фейковая строка? Простой способ 🔐

|Метод|Назначение|Пример|
|---|---|---|
|`randomAlphanumeric(len)`|Случайные буквы + цифры|`"x1Z0B"` ← `randomAlphanumeric(5)`|
|`random(len)`|Любые символы Unicode||